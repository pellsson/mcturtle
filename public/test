os.loadAPI("global")
-- move.initialize()
-- move.goto(135, 5, -57, 0)

internet.connect()
internet.send_ping('*', 2)
-- move.initialize()
-- move.goto(259, 6, 182, move.EAST)

-- move._refuel()
--move.initialize()
--move.forward('attack,dig')
--move.goto(256, 5, 178, 3)
--move.dump_pos()

